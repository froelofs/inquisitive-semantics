{% comment %}
  This is a shorthand for retrieving fields from the funding.yml file.

  Usage:
    {% include getFunding object=fund field='name' %}
{% endcomment %}

{%- include get 
   dataFile=site.data.funding 
   object=include.object
   field=include.field -%}